# Plans

Implementation plans live here, one file per feature: `YYYY-MM-DD-<feature>.md`.

Create one with `/write-plan` from a spec. A plan breaks work into bite-sized,
verifiable tasks with exact file paths and commands, following the
Red-Green-Refactor cycle. This directory is also the configured `plansDirectory`
in `settings.json`.
