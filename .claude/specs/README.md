# Specs

Feature specifications live here, one file per feature: `<feature>.md`.

Create one with `/write-spec` before planning. A spec defines the **Goal**,
**Requirements**, **Acceptance Criteria**, and **Non-Goals** (what you are
explicitly *not* doing).
